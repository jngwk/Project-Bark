/* common */
.header-footer-container {
  position: sticky;
  top: 40px;
  overflow: visible;
  width: 1980px;
  height: 120px;
  margin: 0 auto;
  z-index: 9999;
}
.side-container {
  position: sticky;
  top: 0;
  float: left;
  margin-left: 40px;
  width: 298px;
  height: 1000px;
  border-right: 2px solid var(--color-bean-brown);
}
.side-inner {
  position: relative;
  width: 200px;
  height: 100%;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}
.logo-wrap {
  position: absolute;
  top: -40px;
}
.logo-wrap img:hover {
  scale: 1.2;
  transition: 0.5s;
}

.side-container a {
  z-index: inherit;
}
.footer-wrap {
  position: absolute;
  width: 100%;
  bottom: 30px;
}
.footer {
  text-align: center;
}

.navigation-wrap {
  position: sticky;
  top: 0;
  margin-right: 40px;
  float: right;
  height: 120px;
  width: 1600px;
}
.navigation-inner {
  position: absolute;
  right: 0;
  width: 700px;
  height: 100%;
}
.navigation-inner ul {
  width: 100%;
  height: 120px;
  display: flex;
  justify-content: space-evenly;
  align-items: center;
  position: absolute;
  z-index: 9999;
}

.dropdown-wrap {
  position: relative;
  width: 100%;
}
.dropdown-content {
  position: absolute;
  text-wrap: nowrap;
  padding-top: 2px;
  height: 0;
  overflow: hidden;
  transform: translateY(-15px);
  z-index: 9998;
}
.dropdown-content a {
  display: block;
  text-align: left;
  padding: 4px 0;
}
.nav-item > a > strong{
	color: var(--color-bean-brown);
}
.nav-item > a > strong:hover {
  color: var(--color-effect) !important;
}
.nav-item img:hover{
	cursor: pointer;
	opacity: 0.7;
	transition: .5s;
}
.nav-item:hover .dropdown-content {
  transform: translateY(0);
  height: 500px;
  transition: 1s;
}

.dropdown-content strong {
  font-size: 20px;
  color: var(--color-bean-brown);
}
.dropdown-content strong:hover {
  border-bottom: 2px solid var(--color-bean-brown);
}

.profile{
  position: relative;
  overflow: hidden;
}

.profile-menu{
  position: absolute;
  padding: 10px 20px;
  right: 45px;
  top: 150px;
  background: var(--color-rose-white);
  width: 200px;
  min-height: 330px;
  border-radius: 15px;
  transition: 0.5s;
  visibility: hidden;
  opacity: 0;
  z-index: 9999;
}
.profile-menu.active{
  top: 100px;
  visibility: visible;
  opacity: 0.7;
  backdrop-filter: sepia(90%);
}
.profile-menu::before {
  content: "";
  position: absolute;
  top: -5px;
  right: 28px;
  width: 20px;
  height: 20px;
  background: var(--color-rose-white);
  transform: rotate(45deg);
}
.profile-menu h3 {
  width: 100%;
  text-align: center;
  font-size: 18px;
  padding: 20px 0;
  font-weight: 500;
  color: var(--color-bean-brown);
  line-height: 1.5em;
}
.profile-menu h3 span{
	font-size: 14px;
	font-weight: 500;
	color: var(--color-deep-brown);
}
.profile-menu ul {
	display: block;
}
.profile-menu ul li {
  padding: 16px 0;
  border-top: 1px solid rgba(0, 0, 0, 0.05);
  display: flex;
  align-items: center;
}
.profile-menu ul li img {
  max-width: 20px;
  margin-right: 10px;
  opacity: 0.5;
  transition: 0.5s;
}
.profile-menu ul li:hover img {
  opacity: 1;
}
.profile-menu ul li a {
  display: inline-block;
  color: var(--color-bean-brown);
  font-weight: 500;
  transition: 0.5s;
}
.profile-menu ul li:hover a {
  color: var(--color-effect);
}